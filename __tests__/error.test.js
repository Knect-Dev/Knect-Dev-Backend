'use strict';

const err403 = require('../lib/error-handlers/403');
const err404 = require('../lib/error-handlers/404');
const err500 = require('../lib/error-handlers/500');

describe('403 Error Handler Tests', () => {
  it('should respond with a 403 status and proper error object', () => {

  });
});

describe('404 Error Handler Tests', () => {
  it('should repond with a 404 status and proper error object', () => {

  });
});

describe('500 Error Handler Tests', () => {
  it('should repond with a 500 error status and proper error object', () => {

  });
});