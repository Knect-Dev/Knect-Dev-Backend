'use strict';

describe('Basic Authentication Testing', () => {
  it('should create a new user when POSTing to /signup route with proper fields filled out', () => {

  });
  it('should respond with an error when POSTing to /signup route without all of the proper fields filled out', () => {

  });
  it('should sign in a user when POSTing to /signin route with proper credentials', () => {

  });
  it('should reject a user when POSTing to /signin route with the wrong credentials', () => {

  });


});